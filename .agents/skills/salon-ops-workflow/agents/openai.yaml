interface:
  display_name: "Salon Ops Workflow"
  short_description: "Analyze product-grade salon operations and call flows"
  default_prompt: "Use $salon-ops-workflow to analyze the owner, caller, staff, POS, fallback, dashboard, repeated-run, duplicate-prevention, and safe failure workflow for this request."
