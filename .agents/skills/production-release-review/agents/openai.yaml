interface:
  display_name: "Production Release Review"
  short_description: "Review commercial production readiness"
  default_prompt: "Use $production-release-review to review changes for real production readiness, idempotency, duplicate prevention, service-understanding drift, AI tone guardrails, safe failure states, and critical product invariants."
policy:
  allow_implicit_invocation: true
