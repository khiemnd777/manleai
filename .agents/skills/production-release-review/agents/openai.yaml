interface:
  display_name: "Production Release Review"
  short_description: "Review commercial production readiness"
  default_prompt: "Use $production-release-review to review changes for real production readiness, idempotency, duplicate prevention, service-understanding drift, AI tone guardrails, safe failure states, and critical product invariants. Return findings first with conclusion, work type, evidence, root cause/risk, numbered fix directions, scope/non-goals, checks, and approval gate if implementation is requested."
policy:
  allow_implicit_invocation: true
