interface:
  display_name: "Voice AI Runtime"
  short_description: "Plan AI runtime and voice safety"
  default_prompt: "Use $voice-ai-runtime to investigate AI runtime, tone, service understanding, telephony, realtime, retention, and voice behavior, then plan approval-gated changes with replay safety and safe fallback states."
policy:
  allow_implicit_invocation: false
