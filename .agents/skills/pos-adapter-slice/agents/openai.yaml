interface:
  display_name: "POS Adapter Slice"
  short_description: "Preserve the POSProvider boundary for Square and future adapters"
  default_prompt: "Use $pos-adapter-slice to implement POS integration work without leaking provider-specific logic into booking or AI modules, while handling retry, webhook, sync, idempotency, and duplicate-prevention behavior."
