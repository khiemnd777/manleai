interface:
  display_name: "Salon Dashboard UI"
  short_description: "Plan and review salon dashboard UI"
  default_prompt: "Use $salon-dashboard-ui to investigate or review dashboard UI, produce Mockup as Text for user-facing changes, and wait for explicit approval before frontend edits."
policy:
  allow_implicit_invocation: false
