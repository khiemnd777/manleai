interface:
  display_name: "AI Receptionist Repo"
  short_description: "Navigate product-grade slices in the POS-first salon receptionist monorepo"
  default_prompt: "Use $ai-receptionist-repo to orient around the current architecture, milestone scope, product invariants, salon-scoped AI tone config, idempotency, duplicate prevention, and safe failure states before making changes."
