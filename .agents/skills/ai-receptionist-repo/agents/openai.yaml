interface:
  display_name: "AI Receptionist Repo"
  short_description: "Navigate product-grade slices in the POS-first salon receptionist monorepo"
  default_prompt: "Use $ai-receptionist-repo to investigate current architecture, milestone scope, product invariants, AI tone config, idempotency, duplicate prevention, and safe failure states. Return structured output with conclusion, work type, evidence, root cause or product driver, numbered implementation listing, scope/non-goals, checks, and the exact approval gate before edits."
policy:
  allow_implicit_invocation: false
