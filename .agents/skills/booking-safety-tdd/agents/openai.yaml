interface:
  display_name: "Booking Safety TDD"
  short_description: "Plan booking safety with POS confirmation"
  default_prompt: "Use $booking-safety-tdd to investigate booking behavior, plan the approval-gated test slice, and preserve POS-confirmed appointments plus duplicate-prevention safety."
policy:
  allow_implicit_invocation: false
