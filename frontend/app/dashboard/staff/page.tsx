import { DeferredPage } from "@/components/layout/deferred-page";

export default function StaffPage() {
  return (
    <DeferredPage
      title="Staff"
      milestone="Partially available through Milestone 2 sync"
      description="Square staff sync persists provider-neutral technician records. Full staff controls and availability views are handled after booking operations are in place."
    />
  );
}

